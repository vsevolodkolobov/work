1.Equals и hashCode где используются и что возвращают?

2.Что будет, если мы будем в HashSet использовать свой объект, а ключ - String.

3.Что будет, если наш хэшкод всегда будет возвращать 0?

4.Что будет если equals  переопределен хорошо, а хэшкод всегда возвращает одно и тоже значение?

5. Есть пара с корректным переопределением equals и hascode и мы ее 2 раза подряд пихаем в хэш мап, что будет?

6. Какие виды Collections знаешь?

7. Сложность алгоритмов, Big O ?

8. Сложность добавления в HashMap?

9. Сложность добавления в HashMap хэшкод всегда возвращает 0?

10. Сложность извлечения и HashMapз если хэшкод всегда вернет 0?

11.Сложность добавления элементов в TreeMap?
Ответ: логарифмическая

12.Сложность нахождения элемента в ArrayList?

13.Иерархия исключений в Java?

14.Если исключение есть и в блоке try и в catch, что произойдет?

15.Наш код бросает IOException , нам нужно его обернуть в Runnable, что мы должны сделать?

16.Как понять, должен ли класс быть потокобезопасным?

17.Для чего нужен пулл потоков?

18. У нас есть 10 Runnable, как на 4-х ядерном процессоре выполнить их с миниммум кода?
Ответ: закинуть потоки в экзекьютор сервис и получать результат синхронно по мере выполнения потоков

19. Как в SQL реализовать Many to Many. (нужна соединительная таблица)

20.Есть приложение меню (стандартное меню, у него подменю и т.д. (т.е. дерево)), какой структурой его можно сохранить в БД?
(нужна таблица с данными о иерархии страниц Closure Table , которая будет хранить связи типа потомок-предок) https://habr.com/post/193166/

21.У нас есть форма с 30 полями разных типов. Как реализовать фильтрацию выборки? Т.е. либо фильтруем по 1 либо по всем ?
Ответ: 
4 варианта как можно решить проблему:
1. Дергать  запрос по 1 разу за каждую айдишку (минусы: медленно!)
2. Строить запрос в цикле, в зависимости от числа параметров (минусы: много лишнего кода)!
3. Использовать хранимые процедуры (минусы: разбиваем логику, привязываемся к одной БД!)
4. Использовать готовые Batch (минусы: а вдруг  у нас слишком много различных комбинаций числа айдишек от 1 до 100500)


22.Выгрузить данные из таблицы в CSV файл. По миллиону записей на таблицу. Какие сложности и препятствия видишь?
Ответ: Батчами и в несколько потоков, собираем данные, конкатенируем и пихаем в файл

---------------------------------------------------------------------------------------------

23. Как осуществляется упраление паматью в Java?

24. Какие плюсы и минусы у GC?

25. Области памяти в Java?
Ответ: https://www.baeldung.com/java-permgen-metaspace

26. Откуда беруться утечки памяти в Java? 
Ответ: часто при использовании переменных в Thred (Thread Local переменные), пока у нас живы потоки - вся эта братия будет множиться пока не получим мемори эксепшен.

27. Что такое сервлет и как работает? Как до него дойдет сетевой запрос?
Ответ: http://java-course.ru/student/book1/servlet/

----------------------------------------------------------------------------------------------
28. Нужно написать функцию, которая обходит дерево (задание)

----------------------------------------------------------------------------------------------

29. Как дела с модульными тестами?
 
30. Если ли опыт в Linux?

31. Работал ли с Докером?

32.Этапы сборки в Maven? 

33.Как с мавена запустить приложения на томкэте 
Ответ: https://www.mkyong.com/maven/how-to-deploy-maven-based-war-file-to-tomcat/, т.е. прописываем томкэт плагин в "помке" проекта


34. Как работаешь с Гитом?
Ответ:http://dev-lab.info/2013/08/%D1%88%D0%BF%D0%B0%D1%80%D0%B3%D0%B0%D0%BB%D0%BA%D0%B0-%D0%BF%D0%BE-git-%D0%BE%D1%81%D0%BD%D0%BE%D0%B2%D0%BD%D1%8B%D0%B5-%D0%BA%D0%BE%D0%BC%D0%B0%D0%BD%D0%B4%D1%8B-%D1%81%D0%BB%D0%B8%D1%8F%D0%BD/

35. Какие JMS знаешь, месседж брокеры?
---------------------------------------------------------------------------------------------

36. Опыт с фронтэндом? Отличия margin от pagging? Точка - по классу, решетка по id.
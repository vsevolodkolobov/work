1) Что такое REST и какие есть ограничения в этом подходе?
2) Расскажите про исключения, какие виды и т.п.
3) Что вернется?
try { return 1;}
catch(Exception e) {return 2;} finally{ return 3;}

А так?
try { throw Exception; return 1;}
catch(Exception e) {return 2;} finally{ return 3;}

4) Если на уровне данных возникает SQLException, что с ним делать?
5) Работали ли с многопоточностью?
synchronized(obj) {...} - что значит этот блок? Какие требования к obj?

6) Что такое ConcurrentHashMap, SynchronizedHashMap, отличия?
7) Какая сложность поиска элемента в HashMap?
8) Почему возникают коллизии при вычислении хеша?

9) Hibernate - состояния объекта (transient, persistent, detached)
10) Можно ли обновить объект, который в detached
11) Стратегии наследования в Hibernate?

12) Есть вот такая таблица:
сreate table Documents
(
  id int,
  name varchar(100),
  type_id int,
  create_dt date,
  client_id int
)

Напишите Java entity-класс для маппинга этой таблицы (добавьте аннотации)
13) Напишите SQL запрос, который выведет клиентов, которые за заданную Х дату создали больше 3-х документов.
14) Что такое индекс в БД, зачем нужен? Временная сложность бинарного поиска? Откуда там логарифм?
15) С какими СУБД работали? Какие типы индексов в Postgres есть?

16) Spring: скоупы бинов
Что будет, если в синглтон встроить прототип объект?
17) Какие есть способы autowirинга? Когда какой лучше использовать?

18) Как настроить транзакции в спринге? Какие есть параметры?
19) Какие есть уровни изоляции транзакций?
20) О, вы работали с микросервисами. А какие паттерны там есть и какие реализации?

